import { NavExpandedDirective } from './nav-expanded.directive';

describe('NavExpandedDirective', () => {
  it('should create an instance', () => {
    const directive = new NavExpandedDirective();
    expect(directive).toBeTruthy();
  });
});
